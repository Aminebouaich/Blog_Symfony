{% extends 'base.html.twig' %}
{% block body %}
  <article>
    <h2>{{ article.title }}</h2>
    <div class="metadta">Ecrit le {{ article.createdAt|date('d/m/Y') }} à {{ article.createdAt|date('H:i') }} dans la catégorie Politique</div>
    <div class="content">
      <img src="http://placehold.it/350*150" alt="" />
      {{ article.content|raw }}
    </div>
  </article>
{% endblock %}
